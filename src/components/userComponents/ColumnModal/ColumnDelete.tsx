import { useState } from "react";

import { styled, Select, MenuItem, FormControl, Button } from "@mui/material";

export default function ColumnDelete() {
  const [deletingColumn, setDeletingColumn] = useState<string>("");
  return <></>;
}
